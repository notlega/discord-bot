import type { LegalStatusCodes, BaseResponse } from './response';
import type { CommandInteractionFunction, Command } from './commands';

export type {
  LegalStatusCodes,
  BaseResponse,
  CommandInteractionFunction,
  Command,
};
